# 📊 Rich Tabler
<div align="center">

**Простая и удобная библиотека для создания красивых таблиц в терминале**

[Установка](#-установка) •
[Быстрый старт](#-быстрый-старт) •
[Документация](#-документация) •
[Примеры](#-примеры)

</div>

---

## ✨ Возможности

- 🎨 **Красивые таблицы** — использует [Rich](https://github.com/Textualize/rich) для форматирования
- 📝 **Гибкий ввод данных** — поддержка словарей и кортежей
- ✅ **Валидация типов** — автоматическая проверка данных через [Pydantic](https://docs.pydantic.dev/)
- 🔧 **Расширяемость** — легко наследоваться и кастомизировать
- 📋 **Логирование** — встроенная система логов
- 🎯 **Типизация** — полная поддержка type hints

---

## 📦 Установка

### Локальная установка (для разработки)

```bash
git clone https://github.com/kryscodegod/rich_tabler.git
```
---

## 🚀 Быстрый старт

```python
from rich_tabler import TableMaker, console

columns = ['id', 'name', 'role']
data    = [
    {'id': 1, 'name': 'Кристина', 'role': 'admin'},
    {'id': 2, 'name': 'Данил', 'role': 'user'},
    {'id': 3, 'name': 'Никита', 'role': 'servus dei'},
]

table = TableMaker(columns, data, title = '👥 Пользователи', color = 'cyan').get_table

console.print(table)
```

**Результат:**

```
     👥 Пользователи
┏━━━━━━┳━━━━━━━━━━┳━━━━━━━━━━━━━┓
┃ id   ┃ name     ┃ role        ┃
┡━━━━━━╇━━━━━━━━━━╇━━━━━━━━━━━━━┩
│ 1    │ Кристина │ admin       │
│ 2    │ Данил    │ user        │
│ 3    │ Никита   │ servus dei  │
└──────┴──────────┴─────────────┘
```

---

## 📖 Документация

### Класс `TableMaker`

```python
TableMaker(
    names: list[str],              # Названия колонок
    container: list[dict] | list[tuple] | None = None,  # Данные
    title: str | None = None,      # Заголовок таблицы
    color: str | None = None,      # Цвет текста в колонках
    **kwargs                       # Дополнительные параметры Rich Table
)
```

#### Свойства

| Свойство | Описание |
|----------|----------|
| `get_table` | Возвращает объект `rich.table.Table` |
| `pre` | Показывает структуру данных в виде дерева |

### Форматы данных

#### 📋 Список словарей

```python
data = [
    {'name': 'Item 1', 'value': 100},
    {'name': 'Item 2', 'value': 200},
]

table = TableMaker(['name', 'value'], data)
```

#### 📋 Список кортежей

```python
data = [
    ('Item 1', 100),
    ('Item 2', 200),
]

table = TableMaker(['name', 'value'], data)
```

---

## 🎨 Примеры

### Стилизация таблицы

```python
from rich_tabler import TableMaker, console


data  = [{'product': 'vk-bot', 'price': '666.69р'}]
table = TableMaker(
    names        = ['product', 'price'],
    container    = data,
    title        = '[bold magenta]🛒 Магазин[/]',
    color        = 'green',
    show_header  = True,
    header_style = 'bold cyan',
    border_style = 'bright_blue'
)

console.print(table.get_table)
```

### Наследование для кастомизации

```python
from rich_tabler import TableMaker, console


class MyTable(TableMaker):
    def __init__(self, names, container = None):
        super().__init__(
            names,
            container = container,
            title = '[bold]📊 Мой отчёт[/]',
            color = 'yellow'
        )


    def __call__(self):
        console.print(self.get_table)


# Использование
report = MyTable(['Метрика', 'Значение'], [('Users', 100), ('Messages', 4500)])
report()
```

### Предпросмотр данных

```python
from rich_tabler import TableMaker, console

cols = ['a', 'b', 'c']
rows = [{'a': 1, 'b': 2, 'c': 3}]

table = TableMaker(cols, rows)
console.print(table.pre(cols, rows))  # Выводит структуру данных
```

---

## 🔗 Зависимости

| Пакет | Версия | Назначение |
|-------|--------|------------|
| [rich](https://github.com/Textualize/rich) | ≥14.2.0 | Форматирование вывода |
| [pydantic](https://docs.pydantic.dev/) | ≥2.12.5 | Валидация данных |
| [loguru](https://github.com/Delgan/loguru) | ≥0.7.3 | Логирование |

---
